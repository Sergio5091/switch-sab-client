/**
 * wifiSwitch.js — Driver WIFI réel
 * Phase 6.2 — À implémenter par Sergio
 */

export const allumerPoste = async (posteId) => {
  throw new Error('wifiSwitch.allumerPoste — non implémenté (Phase 6.2)')
}

export const eteindrePoste = async (posteId) => {
  throw new Error('wifiSwitch.eteindrePoste — non implémenté (Phase 6.2)')
}

export const getStatutPoste = async (posteId) => {
  throw new Error('wifiSwitch.getStatutPoste — non implémenté (Phase 6.2)')
}

export const getAllStatuts = async () => {
  throw new Error('wifiSwitch.getAllStatuts — non implémenté (Phase 6.2)')
}
