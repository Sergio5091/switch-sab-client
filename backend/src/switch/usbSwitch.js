/**
 * usbSwitch.js — Driver USB réel
 * Phase 6.1 — À implémenter par Sergio
 */

export const allumerPoste = async (posteId) => {
  throw new Error('usbSwitch.allumerPoste — non implémenté (Phase 6.1)')
}

export const eteindrePoste = async (posteId) => {
  throw new Error('usbSwitch.eteindrePoste — non implémenté (Phase 6.1)')
}

export const getStatutPoste = async (posteId) => {
  throw new Error('usbSwitch.getStatutPoste — non implémenté (Phase 6.1)')
}

export const getAllStatuts = async () => {
  throw new Error('usbSwitch.getAllStatuts — non implémenté (Phase 6.1)')
}
